services:
  sds:
    build: .
    image: sds-image:latest
    container_name: sds
    ports:
      - "8080:80"
      # - "443:443"
    volumes:
      - ./config.yaml:/sds/config.yaml
      - ./data:/sds/data

      # - ./ssl:/etc/nginx/ssl
