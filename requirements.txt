# Runtime dependency manifest for external scanners (Dependabot, etc.).
#
# This mirrors env.yaml — the conda environment that actually builds the app and
# the container — so vulnerability scanners that read requirements.txt see the
# full installed dependency set (including prod-only and dev tooling that
# requirements-test.txt intentionally omits).
#
# This is NOT an install path. The app/container is built from env.yaml via
# conda; the test suite installs from requirements-test.txt. env.yaml is the
# source of truth — keep this in sync with it. Python itself is pinned there to
# 3.12.4 (conda), which requirements.txt cannot express.

# conda-installed, pip-resolvable
pandas==2.2.2
Pygments>=2.15.1

# env.yaml pip: section
Flask==3.1.3
peewee==3.17.5
beautifulsoup4==4.14.2
pyuwsgi==2.0.23.post0
markdown2>=2.4.8
pyyaml==6.0.3
ipython==9.7.0
watchdog==6.0.0
pylint==3.3.1
halo==0.0.31
flask-login==0.6.3
requests
rapidfuzz==3.10.1
python-magic==0.4.27
packaging==25.0
pytz
pytest
